accelerate
datasets
diffusers==0.39.0
huggingface-hub
numpy
pandas
pillow
protobuf
safetensors
scikit-learn
sentencepiece
tokenizers
tqdm
transformers==5.13.0
python-dotenv
pydantic
# vLLM offline inference (Linux/GPU only; DiffusionGemma needs >=0.24.0).
# The wheel is CUDA-specific and MUST match torch's CUDA, or you get
# "libcudart.so.<N>: cannot open shared object file". Install the matching build:
#   pip install vllm --torch-backend=auto   (or =cu128 to match torch 2.x+cu128)
# Set GEMMA_USE_VLLM=0 / DIFFUSION_GEMMA_USE_VLLM=0 to run without it.
vllm>=0.24.0
