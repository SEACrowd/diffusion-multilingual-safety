accelerate
# >=2.16 dropped pa.PyExtensionType, which pyarrow 14+ (pulled by vLLM) removed.
datasets>=2.16.0
diffusers==0.39.0
huggingface-hub
numpy
pandas
pillow
protobuf
safetensors
scikit-learn
sentencepiece
tokenizers
tqdm
transformers==5.13.0
python-dotenv
pydantic
# vLLM offline inference (Linux/GPU only; DiffusionGemma needs >=0.24.0).
# The wheel is CUDA-specific and MUST match torch's CUDA, or you get
# "libcudart.so.<N>: cannot open shared object file". Install with uv and let it
# align both to the driver:  uv pip install -r requirements.txt --torch-backend=auto
# (add --reinstall if a mismatched torch is already present).
# Set GEMMA_USE_VLLM=0 / DIFFUSION_GEMMA_USE_VLLM=0 to run without it.
vllm>=0.24.0
